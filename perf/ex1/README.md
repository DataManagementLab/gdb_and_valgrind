## Profiling a compute-bound program
In this example, we profile a compute-bound program using perf.
The example executes four compute-heavy functions repeatedly (sin & cos).
The expected output show a fairly low-count of 
